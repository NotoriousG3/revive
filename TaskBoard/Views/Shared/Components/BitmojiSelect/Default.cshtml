@using Microsoft.AspNetCore.Mvc.TagHelpers
@model TaskBoard.ViewComponents.BitmojiSelectViewModel
@{
    var items = new List<SelectListItem>() { new("None", "0") };
    items.AddRange(Model.Bitmojis.Select(g => new SelectListItem(g.Name, g.Id.ToString())));
}

@if (Model.ShowLabel)
{
    <label for="@Model.ControlId" class="form-label">Existing Group</label>
}
<select id="@Model.ControlId" asp-items="items" class="form-control text-bg-dark">
</select>