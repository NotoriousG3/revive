@using TaskBoard.ViewComponents
<div id="manageProxyGroupsModal" class="modal fade">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content text-bg-dark">
            <div class="modal-header">
                <h5 class="modal-title"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="row mb-2">
                    <label for="accountGroupModalGroupSelect" class="form-label">Select group to add</label>
                    @await Component.InvokeAsync(nameof(ProxyGroupSelect), new ProxyGroupSelectViewArguments() { ControlId = "proxyGroupModalGroupSelect", ShowLabel = false })
                    <button type="button" class="btn btn-sm btn-outline-light text-bg-dark mt-2" onclick="AddProxyToSelectedGroup()">Add</button>
                </div>
                <hr />
                <div class="row">
                    <h5>Current Groups</h5>
                    <div class="groupContainer"></div>
                </div>
            </div>
        </div>
        <input type="hidden" id="proxyGroupModalProxyId" />
    </div>
</div>